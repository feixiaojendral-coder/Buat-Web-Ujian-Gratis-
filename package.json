{
  "name": "buat-web-ujian-gratis",
  "version": "1.0.0",
  "description": "Web ujian online dengan admin, link ujian, start gate, submit siswa, dan rekap Excel.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check api-handler.js && node --check storage.js && node --check api/[...path].js && node --check app.js && node --check admin_app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
